#include <stdio.h>

int main(void)
{
    int tries = 0;
    int i, j;
    do 
    {
        int i, j;
        tries++;




    } while (i != j);

    printf("%d = %d in %d tries\n", i, j, tries);

    return 0;
}